<Window x:Class="Rot13App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ROT13 Cipher" Height="280" Width="500"
        ResizeMode="CanResizeWithGrip">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Введите текст:" FontWeight="Bold" Margin="0,0,0,5"/>
        <TextBox x:Name="InputTextBox" Grid.Row="1" Height="60" TextWrapping="Wrap"
                 ToolTip="Введите текст для преобразования ROT13"
                 AcceptsReturn="False"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10">
            <Button x:Name="EncryptButton" Content="Зашифровать" Width="120" Height="30"
                    ToolTip="Зашифровать текст методом ROT13"
                    Click="EncryptButton_Click" Margin="0,0,10,0"/>
            <Button x:Name="DecryptButton" Content="Расшифровать" Width="120" Height="30"
                    ToolTip="Расшифровать текст методом ROT13"
                    Click="DecryptButton_Click"/>
        </StackPanel>

        <TextBlock Grid.Row="3" Text="Результат:" FontWeight="Bold" Margin="0,10,0,5"/>
        <TextBox x:Name="OutputTextBox" Grid.Row="4" Margin="0,5,0,0"
                 IsReadOnly="True" TextWrapping="Wrap"
                 ToolTip="Результат преобразования"/>
    </Grid>
</Window>